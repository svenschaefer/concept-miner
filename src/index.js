const { runFromInput, runMain } = require("./run");
const { extractConcepts, validateConcepts } = require("./concepts");

module.exports = {
  extractConcepts,
  validateConcepts,
  runFromInput,
  runMain,
};
