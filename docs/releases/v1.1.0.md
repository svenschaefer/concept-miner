# Release Notes: v1.1.0

Release date: 2026-02-18

## Highlights

- Finalized product-owned Step13 quality stream at `overall_score=100.0` across all six benchmark seeds in default-extended mode.
- Added product-owned deletion-safety tooling:
  - detailed independent benchmark diagnostics
  - standalone concept-candidates determinism/schema checker
  - product PowerShell seed runner
- Added heuristic-level Step13 regression coverage for suppression/promotion behavior.
- Removed `prototype/` directory after porting relevant runtime/tooling/test coverage into product-owned paths.

## Verification Snapshot

- `npm run ci:check` passed.
- `npm run release:check` passed with `RELEASE_TARGET_VERSION=1.1.0`.
