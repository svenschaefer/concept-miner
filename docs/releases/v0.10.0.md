# Release Notes

## Release

- Version: `v0.10.0`
- Date: `2026-02-16`
- Release commit: `989d316`
- Tag: `v0.10.0`

## CI Evidence

- Workflow: `CI` / local release gate
- Result: `success` (`npm run release:check`)

## Smoke Evidence

- Local smoke command: `npm run smoke:release`
- Summary:
  - API exports available: `runFromInput`, `runMain`, `extractConcepts`, `validateConcepts`
  - CLI help contains `Usage:`
  - package `bin` mapping present for `concept-miner`

## Changes Summary

- Added:
  - product-facing `extract` and `validate-concepts` CLI commands
  - frozen reference policy checks and sidecar contract tests
  - release workflow checks for tag/version match and tarball artifacts
- Changed:
  - canonical concepts validation is primary for `validate`, with legacy fallback retained
  - mode naming normalized to product terms (`generic baseline mode`, `default extended mode`)
- Fixed:
  - repository-local prototype path governance after symlink removal
  - OpenAPI/schema required-field drift (`schema_version`, `concepts`, `input_id` constraints)
- Removed:
  - none
