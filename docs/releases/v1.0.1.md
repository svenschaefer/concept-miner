# Release Notes: v1.0.1

Status: released

## Scope

- Corrective patch release after `v1.0.0`.
- Confirms and documents package policy:
  - `prototype/` is reference-only and excluded from npm package payloads.

## Changes

- package/release hardening:
  - explicit ignore hardening for prototype-related artifacts.
  - release documentation updated to make npm payload exclusions explicit.

## Publish Evidence

- npm publish completed: `concept-miner@1.0.1`.
- npm dist-tags verified: `latest` -> `1.0.1`.
- npm deprecation of `1.0.0` remains pending one-time-password (2FA) confirmation.
