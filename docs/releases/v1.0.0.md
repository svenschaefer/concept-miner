# Release Notes: v1.0.0

Status: released

## Scope

- First stable product release for concept-miner 1.x line.
- Canonical concepts-document contract remains primary public contract.
- Default-extended enrichment contract is explicitly typed for:
  - `concepts[*].properties.wikipedia_title_index.exact_match` (`boolean`)
  - `concepts[*].properties.wikipedia_title_index.prefix_count` (`integer`, `>= 0`)

## Compatibility Policy

- Compatibility commands remain supported in 1.x:
  - `concept-miner run`
  - `concept-miner validate`

## Release Gating Checklist

- [x] `npm run ci:check` green on release tree.
- [x] `npm run smoke:release:rehearsal` green.
- [x] Publish posture explicitly confirmed (`private`).
- [x] Changelog release section finalized.
- [x] Release commit pushed and annotated tag pushed (`v1.0.0`).
